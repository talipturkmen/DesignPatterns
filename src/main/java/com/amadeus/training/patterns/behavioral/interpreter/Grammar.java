package com.amadeus.training.patterns.behavioral.interpreter;

public final class Grammar {
	/**
	 * 
	 * 
	 * 
	 * expression ::= <rotation> | <move> | <rotmove>;
	 * rotmove ::= <rotation> then <move>;
	 * rotate ::= right | left
	 * move ::= move <step>*
	 * step ::= [0-9]+
	 */
}
